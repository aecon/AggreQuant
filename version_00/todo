Version 0.0:
X introduce test data and flag to run only tests
X fix folder structure and pickup of correct folder in diagnostics
X add aggregate segmentation
X split aggregates to corresponding cells
X Coded QoI computation
- Verify that QoI are computed correctly. Generate diagnostics for this..
- WIP: MONTAGE WITH OVERLAYS OF CELLS, SEG CELLS, AGGREGATES, and DETECTED CELLS PER AGGREGATE. Relate this with a table of data with number of cells per aggregate, and the opposite, number of aggregates per cell.
- add aggregate diagnostics
- verify file generation

Version 1.0:
- add validation set
- come up with quantitative metrics
- include nnUnet in Segmentation tools
- preprocessing: include file renaming and copying in the python code
- add GPU support



